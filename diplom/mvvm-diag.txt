@startuml

rectangle "Пользовательский интерфейс (View)" as View {
    rectangle "Компоненты интерфейса\n(Vue шаблон / HTML / CSS)" as UI
}

rectangle "Модель представления (ViewModel)" as VM {
    rectangle "Реактивные данные (ref, reactive)" as Reactive
    rectangle "Методы и вычисляемые свойства (computed, methods)" as Logic
}

rectangle "Модель данных (Model)" as Model {
    rectangle "Данные приложения\n(API, база данных, кэш)" as Data
    rectangle "Сервисы и бизнес-логика" as Services
}

UI --> VM : двусторонняя привязка данных\n(data binding / реактивность)
VM --> Model : запрос / обновление данных
Model --> VM : возврат данных
VM --> UI : обновлённые состояния

@enduml
